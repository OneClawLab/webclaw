你有哪些工具可用?

使用 `os_exec` 工具调查 WorkOS 里有什么命令。
请尽最大可能尝试，不要假定存在什么命令。
详细调查这些命令是否存在，用法如何

你有哪些 tools 可用？ (请给出 精确的名称)

使用 os_exec 调查一下 WorkOS 里有哪些命令可用？都怎么用？实际用一下看是否正常。

os_exec `cat /kb/docs/design/Build.md`

在EiduOS 里 使用 os_exec 工具，调查一下 /kb/system/ 下面都是什么内容，对其进行总结。
